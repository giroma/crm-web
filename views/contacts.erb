<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Contacts</title>
  </head>
  <body>
    <h1>contacts</h1>
    <ul>
      <% @list.each do |contact| %>
        <li><%=contact.full_name%> | <%=contact.email%> | <%=contact.note %></li>
      <% end %>
    </ul>
  </body>
</html>
